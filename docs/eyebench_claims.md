# EyeBench Claim Boundary

This release contains non-official EyeBench-style diagnostics only. It is not an official EyeBench submission, not an official leaderboard entry, and not an official benchmark result.

The diagnostic helper reports local aggregate metrics from SAMO outputs to support consistency checks. It must not be described as official benchmark alignment or external validation.
