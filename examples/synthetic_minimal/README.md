# Synthetic Minimal Example

Run the public synthetic pipeline:

```bash
./run.sh
```

The example writes a prepared word table, cross-fitted reader profiles, predictions, metrics, and a run summary to a temporary output directory unless another output path is provided.
